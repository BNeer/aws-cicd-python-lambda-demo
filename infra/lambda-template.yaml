AWSTemplateFormatVersion: '2010-09-09'
Description: Lambda function deployed by CodePipeline

Parameters:
  LambdaCodeBucket:
    Type: String
    Default: python-cicd-artifacts-eu-west-1-123456
    Description: S3 bucket where lambda zip is uploaded
  LambdaCodeKey:
    Type: String
    Default: lambda/lambda_function.zip
    Description: S3 key for lambda zip

Resources:
  DemoLambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: demo-codepipeline-lambda
      Runtime: python3.9
      Handler: app.handler
      Role: arn:aws:iam::198663520156:role/lambda-basic-execution-role
      Code:
        S3Bucket: !Ref LambdaCodeBucket
        S3Key: !Ref LambdaCodeKey
      Timeout: 30
      MemorySize: 128
